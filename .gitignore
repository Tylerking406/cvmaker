# ── Secrets & config overrides ──────────────────────────────────────────────
.env
.env.*
!.env.example
appsettings.*.json          # covers Development, Local, Production overrides, etc.
!appsettings.json           # base config (no secrets) is fine to commit
secrets.json
*.pfx
*.p12
*.pem
*.key
*.cer

# ── .NET build output ────────────────────────────────────────────────────────
bin/
obj/

# ── IDE & editor ────────────────────────────────────────────────────────────
.vs/
.idea/
*.user
*.suo
*.ntvs*
*.njsproj

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
